#include <stdlib.h>
#include<stdio.h>
#include<math.h>

void isPrime(int n){
	if(n==1){
		printf("Not Prime");
	}
	else{
		for(int i=2;i<=sqrt(n);i++)	
			if(n%i==0){
				printf("Not Prime");
				return;	
			}	
	}
	printf("Prime");
}

int main(){
	isPrime(5);
}
	
	
