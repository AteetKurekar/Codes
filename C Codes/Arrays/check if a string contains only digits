#include<stdlib.h>
#include<stdio.h>
#include<string.h>

int main(){
	char a[]="1df234";
	int count=0;
	for(int i=0;i<strlen(a);i++){
		if(a[i]>='0' && a[i]<='9'){
			count++;
		}
	}
	if(count==strlen(a)){
		printf("YES");
	}

//if('B'>='A' && 'A'<='Z'){
//	printf("YES");
//}
}
