#include<stdlib.h>
#include<stdio.h>
#include<string.h>
//char a1[100];
void reverse(char a[],int i,int len){
	if(i<len){
		reverse(a,i+1,len);
		printf("%c",a[i]);
		//a1[len-1-i]=a[i];		
	}
}

int main(){
	char a[]="Ateet";
	reverse(a,0,strlen(a));
	//printf("\n%s",a1);

}
